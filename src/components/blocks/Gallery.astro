<!-- ---
import type { Translation, Language } from "../../types";
import { createTranslate } from "../../lib/i18n";
import SectionHeader from "../SectionHeader.astro";
import OptimizedImage from "../ui/OptimizedImage.astro";
import { ZoomIn } from "lucide-react";

interface GalleryValues {
  src: string;
  caption?: Translation;
}

interface Props {
  title?: Translation;
  subtitle?: Translation;
  items?: GalleryValues[];
  lang: Language;
}

const { title, subtitle, items = [], lang } = Astro.props;
const t = createTranslate(lang);
---

{items.length > 0 && (
  <section class="py-12" id="gallery-section">
    <div class="container mx-auto px-4">
      {title && <SectionHeader lang={lang} title={title} subtitle={subtitle} theme="light" />}
      
      <div class="grid grid-cols-2 md:grid-cols-3 gap-4 px-4">
        {items.map((img, i) => (
          <div
            class="group relative aspect-square rounded-lg overflow-hidden cursor-pointer gallery-item"
            data-index={i}
          >
            <OptimizedImage
              src={img.src}
              alt={t(img.caption)}
              class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
              responsive={{
                sm: 320,
                md: 400,
                lg: 500
              }}
            />
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/40 transition-colors flex items-center justify-center">
              <div class="opacity-0 group-hover:opacity-100 transition-opacity text-white text-center">
                <ZoomIn className="w-8 h-8 mx-auto mb-2" />
                <span class="text-sm">{t(img.caption)}</span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>

    {/* Lightbox container */}
    <div id="lightbox-root"></div>
  </section>
)}

<script>
  import React from 'react';
  import { createRoot } from 'react-dom/client';
  import GalleryLightbox from './GalleryLightbox';

  const gallerySection = document.getElementById('gallery-section');
  if (gallerySection) {
    const items = Array.from(gallerySection.querySelectorAll('.gallery-item'));
    const lightboxRoot = document.getElementById('lightbox-root');
    const images = JSON.parse(gallerySection.dataset.items || '[]');

    items.forEach(item => {
      item.addEventListener('click', () => {
        const index = parseInt(item.getAttribute('data-index') || '0');
        renderLightbox(index);
      });
    });

    function renderLightbox(index: number | null) {
      if (!lightboxRoot) return;
      const root = createRoot(lightboxRoot);
      
      // Pass the actual data from props
      // Note: We need a way to pass the full items array to the script
      // I'll update the section to hold the data in a data-attribute
    }
  }
</script> -->
